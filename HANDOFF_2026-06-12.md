# HANDOFF COMPLETO — Sessão 08/06 → 12/06/2026

> **Para a próxima sessão: leia este documento INTEIRO antes de qualquer ação.**
> Depois consulte as memórias persistentes (índice na seção 11) e o `CLAUDE.md`.
> Este arquivo é o snapshot mais recente e **sucede** o `HANDOFF_2026-06-03.md`.
> Regra do Leonardo: **nenhuma informação detalhada das sessões pode se perder.**

---

## 0. PERSONA E REGRAS INVIOLÁVEIS

**Quem você é:** Engenheiro de Produção sênior (PCP, MRP, BOM, Curva ABC, lead time, base-stock/order-up-to, estoque×fluxo de Forrester) **e** desenvolvedor sênior (Python, Streamlit, Postgres, APIs REST, LLMs/tool use). Técnico, direto, não simplifica, defende decisões com argumento (vira capítulo de TCC), antecipa 2-3 jogadas.

**Quem é o Leonardo:** Eng. de Produção UFCG (10º período, matrícula 121111053), TCC + estágio simultâneos. **NÃO programa** — explique pelo efeito visível, sem jargão de CSS/DOM/commit. Termos de PCP são OK. Ele lê tudo, teme perder dados, e **ensina o fluxo da fábrica continuamente** (incorpore esse aprendizado; não modele regra rígida — a fábrica é cheia de variáveis e exceções).

**Regras que NÃO se quebram:**
1. **Sem nomes próprios** em prose/UI/código/doc ativa → departamentos: **Gestão, Produção, Corte, Embalagem, Suprimentos**. Mapeamento real (só pra você saber): Eraldo=Gestão (sócio-gerente), Sr. Joel=Produção, Gil/Giovanni=Corte/Cocada assada, Carlos/Francisco/Popô=Corte/Embalagem, Maria=Palha, Mariana=Suprimentos. **EXCEÇÃO formal:** fichas oficiais do TCC/estágio usam nome civil — supervisor **Eraldo**, orientador **Prof. Kegenaldo**.
2. **Documentos acadêmicos: ZERO menção a IA/assistente/rascunho.** Tudo deve ler como escrito pelo Leonardo.
3. **O sistema SUGERE, a Gestão DECIDE.** Sempre.
4. **Honestidade sobre limitações.** Não inventar números/métricas não medidas. Não fabricar citação.
5. **Commit/push só quando pedido.** Mensagem termina com `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Branch `main`.
6. **Segredos NUNCA no chat/git/memória** — só em `.streamlit/secrets.toml` (local, gitignored) e nos Secrets do HF. Vale pra senha do banco, ANTHROPIC_API_KEY e token SIGE.
7. Encoding Windows: usar `PYTHONIOENCODING=utf-8` em scripts com acento.
8. **`secrets.toml` local aponta pra PRODUÇÃO** (us-east-1) → NÃO rodar o app local (escreveria no banco real). Scripts de leitura: ler `DATABASE_URL`/`SIGE_*` do secrets e setar em `os.environ` antes de importar — NUNCA imprimir os valores.

---

## 1. O PROJETO EM 30 SEGUNDOS

PCP digital para **Doces Vó Nena** (razão social *Pequenas Mordidas Alimentos Ltda*, CNPJ 22.765.961/0001-20, **produtora**; distribuição por *Soglia Ind. e Com.*, CNPJ 04.846.092/0001-86; Rua Dois de Julho 241, Ipiranga, São Paulo-SP). Confeitaria semi-industrial. Substitui papel por sistema digital com: folha diária, painel, análises (Curva ABC, anomalias por Isolation Forest, média móvel, **+ análise da Bala**), sugestão automática (palha e cocada), Suprimentos/MRP, **assistente de IA** (10 ferramentas + briefing proativo — LIGADO nesta sessão) e **integração SIGE Cloud** (read-only — CONECTADA nesta sessão). Base do TCC + relatório de estágio. Defesa **~16–21/07/2026**.

---

## 2. STACK E INFRA

- **App:** Python 3.14 + Streamlit multi-page. Entry: `app.py` (`st.navigation` com lista EXPLÍCITA de páginas em grupos — a pasta `pages/` NÃO é auto-descoberta; toda página nova precisa ser registrada em `app.py`).
- **Banco:** Postgres (Supabase), **produção = us-east-1** (projeto `pcp-vo-nena-us`). O `sa-east-1` antigo está obsoleto.
- **Hospedagem:** **Hugging Face Spaces** — `huggingface.co/spaces/leonardosoglia/pcp-vo-nena` (Docker SDK).
- **Git remotes:** `origin` (github.com/leonardosoglia/pcp-vo-nena, privado) + `hf` (deploy). Push em `hf` dispara rebuild (~minutos). LFS rastreia `*.pdf,*.docx,*.xlsx` (NÃO `.jpeg`).
- **Cache:** `cached_db.py` (`@st.cache_data`). UI importa daqui; scripts CLI importam `database.py` direto.
- libs: pandas, scikit-learn, plotly, python-docx, **anthropic** (assistente), **requests** (SIGE).

---

## 3. O QUE ESTA SESSÃO FEZ (por frente)

### 3.1 Assistente de IA — LIGADO, corrigido, auditado, em uso (commit `9d6f7bc`)
- **Chave:** Leonardo pôs **US$ 10 de crédito** no console.anthropic.com e a `ANTHROPIC_API_KEY` no Secret do HF. Assistente no ar.
- **Correções de pré-deploy (achadas antes de ligar — o código nunca tinha sido exercido):**
  - Seletor de modelo `claude-opus-4` (deprecado, ia ser desligado 15/06) → **`claude-opus-4-8`**.
  - Pricing do Opus em `estimar_custo` era $15/$75 (do Opus 4.0) → **$5/$25** (Opus 4.8).
  - **Bug crítico `_resumir_folha`**: `pmbd = list(get_pm_balas_doces(data))` tratava o **dict como lista** → quebraria a montagem de contexto em QUALQUER dia com PM/balas. Corrigido pra `pmbd = get_pmbd(data)` (dict).
  - Bug do custo do briefing em `pages/7` (estimar_custo com `modelo` na posição errada).
  - **Auditoria adversarial** (workflow multi-agente) achou mais 2 reais: (a) `perguntar_com_tools` não tratava `stop_reason='max_tokens'` → resposta válida virava "erro" (corrigido — agora devolve o texto parcial); (b) cache de prompt nunca ativava no caminho Haiku/streaming (system ~2.4k < mínimo 4096) → ajustei o texto da UI pra ser honesto.
- **Calibração (efeito real):** o assistente passou a **enxergar as "Observações do dia" + a contagem de pessoas por área** (estava cego); ensinei o SYSTEM_PROMPT a usar isso (explicar dia atípico, não alarmar quando equipe reduzida justifica). Alinhei o **peso da bandeja**: 6/5,5 kg físico vs **7 kg** base de conversão de pote.
- **Modelos válidos (confirmados via skill claude-api):** `claude-haiku-4-5`, `claude-sonnet-4-6`, `claude-opus-4-8` (NÃO usar date-suffix). Briefing default = Sonnet.
- **1º briefing rodou — EXCELENTE:** usou as observações/pessoas, cruzou 4 ferramentas, e **pegou sozinho o estoque negativo de insumos** (mesmo diagnóstico que o Claude). Custou **R$ 0,12**. É material direto do Cap. 5.

### 3.2 Catálogo de Produtos (SKUs) + 92 fotos (commit `6b64776` parcial)
- Planilha **`CADASTRO DE PRODUTOS (SKUs).xlsx`** lida e copiada pro projeto (`entrevistas/`).
- **Correções importantes ao entendimento:**
  - **Cocada Zero = ALTO volume** (Mini 27g, no nível da Leite Condensado) — NÃO baixo volume. Alta margem → prioridade máxima.
  - **Potes ("Cremosa", vidro)** existem nos **6 sabores** (260g e 605g), não só T/L/Z.
  - **Mini = 30g** (sabores normais), **27g só no Zero**. (A planilha dizia 27g pra todos — imprecisa; os rótulos confirmam 30g.)
  - **Palha tablete: gramaturas mistas** — Chocolate 45g, Leite em Pó 45g, **Churros 50g** (rótulos lidos).
  - **Palha "Tradicional" = sabor Chocolate; "Leite em Pó" = Ninho.** Display 50g = 4 chocolate + 4 ninho + 2 churros.
  - **Cocada Pet existe** ("Cocada em Cubos" 160g; Zero Pet 100g) — a planilha não tinha, mas o sistema e os rótulos confirmam.
  - **Pão de Mel = sortido** (Doce de Leite, Cocada, Ganache), pedaço 60g, display 10×60g.
  - **Cocada Assada** = "na Cumbuca" 145g, casca de coco, receita com **gema de ovo** (único produto com ovo — alérgeno), shelf ~40 dias.
  - **Produtos terceirizados** (marca Vó Nena, produzidos FORA): Bala de Coco (Tradicional, Beijinho), Goiabada Cascão, Doce de Leite (cubos/barra). NÃO entram no PCP de produção; entram em vendas/estoque acabado.
  - **Brownie 100G** apareceu no SIGE — produto fora do nosso catálogo (a confirmar).
- **92 fotos organizadas POR FORMATO** em `entrevistas/fotos_produtos/`: `01_Cocada_Tablete_45g`, `02_Cocada_Mini_30g` (Zero_27g), `03_Cocada_Pet`, `04_Cocada_Pote_Cremosa`, `05_Cocada_Assada`, `06_Palha_Tablete_50g` (+_Display), `07_Palha_Pet`, `08_Pao_de_Mel` (+_Display), `09_Bala_Doce_de_Leite`, `10_Doce_de_Leite`, `11_Terceirizados`, `_A_CONFERIR`. Cada pasta-produto tem `descricao.md` (modelo). **As fotos NÃO estão no git** (não configuradas pra LFS — ficam no OneDrive). Leonardo organizou as faltantes (zerou o `_A_CONFERIR`).
- **Lead times variáveis:** cocada 3–5 dias (ideal 3), palha ~3, potes ~1, **bala ~8**. → pede estoque de segurança, não ponto fixo.
- Registros: `entrevistas/02_produtos_e_lead_times.md` (catálogo completo + tabelas), memória `project_catalogo_produtos`.

### 3.3 Bala de Doce de Leite — lógica + página (commit `6b64776`)
- **Lógica de produção (regra de negócio, do Leonardo) — REFERÊNCIA, não lei:** política **base-stock** — alvo ~1.000–1.100 balas; **menos estoque → mais tachos** (~900→4 tachos · ~1.000→3 · ~1.100→2). **Tachos compartilhados com a cocada** (teto de capacidade). **Cozimento diário** (pipeline; lead ~8d = processo do lote). **A fábrica oscila MUITO** — imprevistos, muitas variáveis.
- **Descoberta-chave:** `cnt_balas` (histórico 0–205) é **SÓ a prateleira embalada**. O **total** que a Gestão usa pra decidir (~1.000) inclui as **"balas prontas pra cortar"** — camada que o **sistema NÃO captura** (análogo às 3 camadas da cocada). Um "estoque 0" **não é ruptura** (tinha pra cortar).
- **Visão do Leonardo (norte do sistema):** conforme um produto **vende mais, focar capacidade nele** (produção puxada pela demanda). Precisa do módulo de vendas.
- **Página `pages/12_Bala.py`** (grupo Análises): cartões, produção diária + média móvel, estoque-prateleira (com zeros marcados), produção×estoque (proxy de giro), linha do tempo. **Honestidade embutida** (estoque parcial; regra é referência). Dados reais: **28/33 dias com produção, 76 tachos = 2.280 balas**.
- **A clarear com a Produção:** o que `cnt_balas` mede exatamente; como contar as "balas pra cortar" (pra depois ligar a sugestão automática base-stock).

### 3.4 SIGE Cloud — CONECTADO (read-only) ⭐
- **Causa do erro antigo RESOLVIDA:** o token que o Leonardo tinha (~3-4 semanas) era de um **equipamento SAT (fiscal)**, NÃO da API REST de dados → por isso "Acesso não autorizado" (HTTP 403). **Nosso código (`sige_cloud_api.py`) estava certo o tempo todo.**
- **Suporte SIGE (atendente Jéssica Massing, via chat) criou a API correta** e mandou um **token NOVO permanente (não expira)**. Token (256 chars, começa `56`, fim `c27b`) + `User=eraldosoglia@docesvonena.com.br` + `App=API` estão no `.streamlit/secrets.toml` LOCAL (`SIGE_AUTH_TOKEN`/`SIGE_USER`/`SIGE_APP`/`SIGE_DEPOSITO_PADRAO`). **Token NUNCA vai pro git/chat/memória.**
- **`testar_conexao()` = OK.** Lendo produtos reais: cod10 "TABLETE COCADA TRADICIONAL 30G", cod18 "COCADA CUBOS TRADICIONAL 160G", cod74 "BROWNIE 100G".
- **Schema do produto SIGE** (campos reais): ID, UltimaAlteracao, Categoria, CategoriaID, Marca, Fornecedor, Codigo, Ean, Prateleira(s), Nome, Modelo, Genero, EstoqueUnidade, Especificacao, PesoKG, PrecoCusto, LucroDinheiro/Percentual, PrecoVenda, PrecoMinimoVenda, EstoqueRisco, DepositoPadrao, EstoqueSaldo.
- **Arquitetura: Modelo B read-only.** Escrita travada por código (`SIGE_PERMITIR_ESCRITA`). Endpoints leitura: `Produtos/GetAll`, `Produtos/Pesquisar`, `Estoque/BuscarQuantidades`. Base `https://api.sigecloud.com.br/request`. Doc `https://api.sigecloud.com.br/swagger/ui/index`. Sem limite de requisições; 1 credencial cobre todos CNPJs.
- **A "Central de Integrações" do SIGE (Lojas/Pedidos/Sincronizações) é e-commerce** — NÃO a API REST nossa.
- **PAYOFF:** o SIGE tem `EstoqueSaldo` real dos insumos → trazer isso **resolve o estoque negativo de insumos** que o assistente reclamou no briefing, e liga a ferramenta de insumos.

---

## 4. ESTADO ATUAL — ONDE ESTAMOS

| Frente | Estado |
|---|---|
| Folha, Painel, Sugestões (palha/cocada), Suprimentos/MRP (estrutura) | ✅ no ar |
| Assistente de IA (10 tools + briefing) | ✅ LIGADO, corrigido, auditado, 1º briefing rodou |
| Análise da Bala (`pages/12_Bala.py`) | ✅ no ar (subindo no rebuild do commit 6b64776) |
| Catálogo de produtos (SKUs + fotos + lead times) | ✅ documentado |
| **SIGE Cloud (API read-only)** | ✅ **CONECTADO local** · ❌ falta token no HF (produção) + a ponte API→banco |
| Estoque inicial de insumos (resolve o negativo) | ❌ pendente — virá do SIGE |
| Módulo de Vendas | ❌ não começou (próximo grande módulo p/ fechar o ciclo) |
| Capacidade/polivalência da equipe | ❌ mapeada (Leonardo deu o cross-training), não modelada |
| Camada "balas pra cortar" | ❌ a clarear com a Produção |
| Escrita TCC / relatório | em andamento (docs commitados, editados no Word) |

---

## 5. PRÓXIMOS PASSOS (em ordem) — começar pelo SIGE

1. **SIGE — continuar (foco imediato):**
   - (a) **Mapear o catálogo** read-only: `pesquisar_produtos(categoria="PRODUÇÃO")` e `"EMBALAGEM"` (= insumos) + listar produtos acabados. Entender o que tem lá. Acertar o nome real do depósito (`SIGE_DEPOSITO_PADRAO`).
   - (b) **Pôr o token no Secret do HF** (`SIGE_AUTH_TOKEN`/`SIGE_USER`/`SIGE_APP`) pra a API valer em produção.
   - (c) **Construir `importar_sige_api.py`** — espelho read-only do `importar_csv_sigee.py`, puxando da API: mapeia produto SIGE → tabela `insumos` (custo, saldo, fornecedor). Idempotente, com dry-run. **Só lê do SIGE, só escreve no NOSSO banco.** → resolve o estoque negativo de insumos.
   - (d) Botão "Sincronizar com SIGE" na página Suprimentos.
2. **Vendas** — ler a planilha de vendas consolidada → giro real + Curva ABC por **margem** (não só volume) + fecha o ciclo de PCP. (Streamlit não é ideal pro vendedor; ler a planilha que já existe é o caminho.)
3. **Capacidade dinâmica / polivalência** — modelar a matriz de cross-training no módulo Equipe → o briefing para de tratar headcount como rígido.
4. **Bala** — capturar a camada "pra cortar" → ligar a sugestão automática base-stock.
5. **TCC/relatório** — aprofundar capítulos; responder o orientador.

---

## 6. ARQUIVOS-CHAVE (mapa)

| Arquivo | Função |
|---|---|
| `database.py` | Schema v2, dual-backend, toda lógica de dados (puro). |
| `cached_db.py` | Wrappers `@st.cache_data`. UI importa daqui. |
| `app.py` | Entry `st.navigation` — **registrar toda página nova aqui** (em grupo). |
| `lancamento.py` | Folha diária (cocada/palha/papelzinho/PM-balas/observações/pessoas). |
| `pages/12_Bala.py` | **NOVO** — análise da Bala. |
| `pages/7_Assistente_IA.py` · `claude_assistant.py` · `assistant_tools.py` | Assistente de IA (motor + 10 tools + UI). |
| `sige_cloud_api.py` | Cliente SIGE read-only (testar_conexao, listar_produtos, pesquisar_produtos, buscar_saldo_estoque). |
| `importar_csv_sigee.py` | Importa planilha SIGE → insumos (plano B). `importar_sige_api.py` = a criar (via API). |
| `cocada_planejamento.py` · `palha_planejamento.py` | Sugestões (Camada 2). |
| `entrevistas/02_produtos_e_lead_times.md` | **Catálogo completo de SKUs + lógica da bala.** |
| `entrevistas/fotos_produtos/` | 92 fotos por formato (fora do git). |
| `entrevistas/CADASTRO DE PRODUTOS (SKUs).xlsx` | Planilha-fonte. |
| `suprimentos_sigee/03_solicitar_credenciais_api.md` | Processo de credenciais SIGE. |
| `CLAUDE.md` · `CADERNO.md` | Regras · diário. |

---

## 7. FATOS TÉCNICOS / REGRAS CRÍTICAS

- **Cortados² = emb + cort1 + joel** (joel_pet em bandejas ×30, ×60 p/ Z). Base pra avaliar cobertura do param.
- **Estoque × Fluxo (Forrester):** `emb_*`/`cort1_*` = estoque (não somar entre dias); `ord_*` = fluxo (pode somar).
- **Não-acomodação:** Gestão pede corte/produção mesmo com param coberto (decisão cognitiva).
- **Bala:** `ord_balas` em TACHOS (1 tacho = 30 balas), `cnt_balas` = só prateleira. Base-stock alvo ~1000-1100 (referência). Tachos compartilhados c/ cocada. Lead ~8d, pipeline diário.
- **Palha:** "Tradicional"=Chocolate, "Leite em Pó"=Ninho. Tablete 45g (choco/ninho) e 50g (churros). 1 receita = 1 bandeja (NÃO tacho).
- **Cocada:** Zero NÃO tem 45g; Zero é alto volume (mini 27g). Bandeja 45g=100und · Mini=150 · Pet=30 (Z=60) · 1 tacho=8 band (Z=3). Bandeja ~6kg físico / 7kg conversão pote.
- **SIGE:** read-only, token permanente no secrets local; schema do produto na seção 3.4.

---

## 8. COMMITS DESTA SESSÃO (todos em `main`, pushados origin+hf)
```
6b64776 feat(analise): pagina da Bala de Doce de Leite + catalogo de produtos (SKUs)
9d6f7bc fix(ia): prepara assistente pro deploy real (modelos, custo, bug pmbd, observacoes, max_tokens)
ebf92cd docs: handoff completo da sessao 28/05 -> 03/06
```
Untracked (não commitar sem decidir): `_cocada_head.py`, `_validar_cocada_*.py` (scripts antigos), `entrevistas/fotos_produtos/` (92 fotos — ficam no OneDrive).

---

## 9. SEGURANÇA
- `.streamlit/secrets.toml` (DATABASE_URL+senha, ANTHROPIC_API_KEY se houver, **SIGE_AUTH_TOKEN/USER/APP**) está no `.gitignore`. NUNCA commitar/imprimir.
- Token SIGE é **permanente** — guardar bem. Se vazar, pedir novo ao suporte.
- Para produção (HF), os mesmos segredos vão nos Secrets do Space.

---

## 10. CRONOGRAMA TCC/ESTÁGIO
- Estágio 28/04 → 26/06/2026 (240h). Defesa **~16–21/07/2026**.
- TCC científico ABNT (incompleto de propósito até Metodologia) + Relatório operacional ABNT. Não copiar entre si.

---

## 11. MEMÓRIAS PERSISTENTES (índice em `~/.claude/.../memory/MEMORY.md`)
Chave desta fase: `project_catalogo_produtos` (SKUs + lógica da bala), `project_sige_estado` (jornada SIGE → conectado), `project_handoff_sigee`, `project_pessoa_mariana_e_sigee`, `feedback_sem_nomes`, `feedback_validar_ui`, `feedback_comunicacao_simples`, `feedback_persistir_dados`, `project_tcc_vs_relatorio_direcao`, `project_validacao_cocada_28-05`, `project_migracao_hf_spaces`, `project_fase1_ml_completa`.

---

## 12. PRIMEIRA AÇÃO DA PRÓXIMA SESSÃO
1. Ler este handoff INTEIRO + as memórias + `CLAUDE.md`.
2. Confirmar que o SIGE conecta (`sige_cloud_api.testar_conexao()` — token já no secrets local).
3. **Retomar o SIGE pelo passo 5.1(a): mapear o catálogo** (categorias PRODUÇÃO/EMBALAGEM) read-only.
4. Não decidir o rumo sozinho — seguir a ordem da seção 5, confirmando com o Leonardo.

*Fim do handoff. A próxima sessão tem tudo aqui + memórias + CLAUDE.md.*
