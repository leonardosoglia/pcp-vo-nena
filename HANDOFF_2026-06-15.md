# HANDOFF COMPLETO — Sessão 14/06 → 15/06/2026 (SIGE → Custo → Margem → Vendas)

> **Para a próxima sessão: leia este documento INTEIRO antes de qualquer ação.**
> Depois consulte as memórias persistentes (índice no fim) e o `CLAUDE.md`.
> Sucede o `HANDOFF_2026-06-12.md`. Regra do Leonardo: **nenhuma informação pode se perder.**

---

## 0. PERSONA E REGRAS INVIOLÁVEIS

**Quem você é:** Engenheiro de Produção sênior (PCP, MRP, BOM, Curva ABC, custo/margem, lead time, base-stock, estoque×fluxo de Forrester) **e** desenvolvedor sênior (Python, Streamlit, Postgres, APIs REST, LLMs). Técnico, direto, não simplifica, **questiona números que não fazem sentido** (o Leonardo valoriza isso), defende decisões com argumento, antecipa jogadas.

**Quem é o Leonardo:** Eng. de Produção UFCG (10º período), TCC + estágio simultâneos na Doces Vó Nena (família Sóglia). **NÃO programa** — explique pelo efeito visível, sem jargão de código. Ensina o fluxo da fábrica continuamente (incorpore; nada é regra rígida).

**Regras que NÃO se quebram:**
1. **Sem nomes próprios** em prose/UI/código/doc → departamentos (Gestão, Produção, Corte, Embalagem, Suprimentos). Nomes de EMPRESAS (Soglia Indústria, Pequenas Mordidas) são ok. EXCEÇÃO formal: fichas do TCC/estágio usam nome civil (supervisor Eraldo, orientador Prof. Kegenaldo).
2. **Documentos acadêmicos: ZERO menção a IA.** Tudo deve ler como escrito pelo Leonardo.
3. **O sistema SUGERE, a Gestão DECIDE.** Sempre.
4. **Honestidade sobre limitações.** Não inventar números/métricas. Se houver disparidade, APONTAR e fazer pergunta pra fábrica.
5. **Commit/push só quando pedido.** Mensagem termina com `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Branch `main`. ⚠️ **Push pra main/HF (produção) exige autorização EXPLÍCITA do Leonardo** — o classificador de segurança barra "prossiga" genérico; precisa de "sim, pode enviar/publicar".
6. **Segredos NUNCA no chat/git/memória** — só em `.streamlit/secrets.toml` (gitignored) e Secrets do HF. Vale pra senha do banco, ANTHROPIC_API_KEY e token SIGE.
7. **CRAVAR TUDO na memória** (reforçado 15/06): não só dados da fábrica — **dado, dúvida em aberto, decisão, disparidade, esclarecimento**. A cada achado relevante, gravar na hora.
8. **`secrets.toml` local aponta pra PRODUÇÃO** (us-east-1) → NÃO rodar o app local (escreveria no banco real). Scripts de leitura: bootstrap `DATABASE_URL`/`SIGE_*` do secrets pro `os.environ`, NUNCA imprimir valores. Encoding Windows: `PYTHONIOENCODING=utf-8`.

---

## 1. O PROJETO EM 30 SEGUNDOS

PCP digital para **Doces Vó Nena** (confeitaria semi-industrial, São Paulo-SP; grupo com 5 CNPJs — produção sob *Soglia Indústria*). Substitui papel por sistema digital. Base do TCC + relatório de estágio. **Defesa ~16–21/07/2026.** App em produção no **Hugging Face Spaces** (Docker), banco Postgres us-east-1, assistente de IA ligado. **Esta fase: integração SIGE Cloud (ERP) read-only + a cadeia Custo → Margem → Vendas → Lucro.**

---

## 2. STACK E INFRA

- **App:** Python 3.14 + Streamlit multi-page. Entry `app.py` (`st.navigation` — TODA página nova precisa ser registrada lá).
- **Banco:** Postgres (Supabase) us-east-1. **Hospedagem:** `huggingface.co/spaces/leonardosoglia/pcp-vo-nena` (Docker).
- **Git:** `origin` (github.com/leonardosoglia/pcp-vo-nena) + `hf` (deploy; push em `hf main` reconstrói o Space). LFS rastreia `*.pdf,*.docx,*.xlsx`.
- **SIGE Cloud:** `sige_cloud_api.py` (cliente read-only). Token permanente nos Secrets do HF (configurado 15/06) e no secrets.toml local. Base `https://api.sigecloud.com.br/request`.
- libs: pandas, plotly, anthropic, **requests** (SIGE).

---

## 3. O QUE ESTA SESSÃO FEZ (a cadeia completa do PCP, ligada ao SIGE)

### 3.1 De-para SIGE ↔ 33 insumos do BOM (`suprimentos_sigee/de_para_sige.md`)
Mapeou cada insumo do BOM ao código SIGE, via **workflow multi-agente adversarial** (gerar + refutar + completude). **6 prontos · 12 a escolher código (duplicatas) · 10 a confirmar unidade · 5 a cadastrar** (mascavo, essência de mel, cravo, amaciante, etiqueta). **Regra de ouro:** o saldo real vive no cadastro "sujo" sem-categoria, não no "bonito" PRODUÇÃO (zerado). **Armadilha pega:** o cacau tinha sido mapeado pro código `19375` que era nº DE LINHA do catálogo, não código (real `82143`) → validar TODO código contra o ERP. Unidade de compra ≠ consumo erra 10-25× (sorbato saldo 224 "caixas"=5,6t → é kg). §7 do doc tem as perguntas pra Suprimentos.

### 3.2 Ponte de custos (`importar_sige_api.py`) — APLICADA EM PRODUÇÃO
Importa CUSTO + IDENTIDADE + FORNECEDOR dos insumos do SIGE (read-only no SIGE). Converte custo de compra→receita (PrecoCusto÷fator) com sanity check. Rodada `--apply`: **20 custos reais aplicados** (antes todos R$0,00). NÃO toca estoque (carga inicial vem da contagem física — 3 camadas Forrester).

### 3.3 Arquitetura SIGE definida com a Gestora (`docs/ARQUITETURA_SIGE.md`, `requisitos_gestora_sige.md`)
Decisões confirmadas: **(1) OP entra MANUAL** → integração 100% read-only (ramo de escrita isolado/bloqueado em `sige_cloud_api`); (2) **fichas técnicas = fonte única no SIGE**, nós só lemos; (3) decisão de produção é HUMANA; (4) depósito da fábrica = **FABRICA**. PCP do SIGE estava VAZIO (sem OPs). Plano: cadastrar **OP de teste manual** pra validar grandezas/rendimento.

### 3.4 Reconciliação de estoque (`reconciliacao_sige.py`, `pages/13_Reconciliacao_SIGE.py`) — NO AR
Compara estoque SIGE × nosso sistema, insumo a insumo. **Usa o saldo CONSOLIDADO do GetAll** (a fábrica é UM local físico; depósitos/CNPJs são divisões contábeis — NÃO ler depósito isolado). Tela registrada no app (grupo Cadastros). **Deploy no HF (commit `44ce7f3`) — testada em produção, funcionando com o token.** Hoje mostra tudo "divergente" porque o nosso estoque está negativo (auto-baixa sem carga inicial) — esperado até a contagem física.

### 3.5 Fichas técnicas pro SIGE (`docs/fichas_tecnicas_para_sige.md`)
13 fichas (6 cocadas, 5 palhas, PM, bala) geradas do BOM × de-para, **verificadas adversarialmente (workflow, 0 erros)**. Cada uma com insumo + código SIGE + qtd + rendimento — pronta pra cadastro manual no SIGE. **Docinhos pendente** (sem receita no BOM).

### 3.6 Custo de produção (`custo_producao.py`)
Custo por receita/tacho × custo de insumo. Cocada Tradicional R$202/tacho (R$0,25/tablete 45g), Leite Cond. R$456, **Zero R$348 e R$0,77/und = A MAIS CARA** (adoçantes + rende só 3 band). Palhas com custo PARCIAL (faltam creme/manteiga/maizena — os "a confirmar unidade" do de-para).

### 3.7 Margem (`margem_produto.py` + preços reais)
**LIÇÃO (Leonardo desconfiou do 96%, com razão):** a "margem de matéria-prima" é ENGANOSA — alta sempre (82-96%) porque a MP é fração pequena do preço; **NÃO é lucro**. O custo TOTAL é o que importa. Margem real por canal: cocada atacado ~74%/varejo ~88%; **PM atacado SÓ 36%** (mão de obra alta). Preços reais (em `entrevistas/precos_revenda_varejo_2026-06-15.md`): ATACADO ~metade do VAREJO; "com ST" (nota) = "sem ST" +~13% (imposto).

### 3.8 Vendas do SIGE (`vendas_sige.py`) — DESCOBERTA-CHAVE
**O SIGE TEM as vendas reais** → módulo de Vendas SEM planilha. `Pedidos/Pesquisar` (itens em `Items`; canal em `Tabela`; pageSize MÁX 100; só StatusSistema "Faturado"). Teste 1 semana (08-15/06): **1560 pedidos, R$98k receita, 91 produtos, Curva ABC (28 produtos=80%)**. Campeões: tablete cocada trad 45g (2333un/R$7,3k), Cocada Cubos 160g (~R$15k), bala DL, PM. (OperacoesPDV = operações de caixa, não itens.)

### 3.9 Lucro/contribuição (`lucro_produto.py`) — CICLO FECHADO (parcial)
Cruza vendas × custo = contribuição de MP por produto. **Cobre só 16,9% da receita** (falta custo dos formatos que mais vendem: Cubos 160g, Bala). Tablete trad lidera (R$6,7k contrib/sem); **PM contribui R$3,8k vendendo 5× menos = decidir por retorno, não volume** (a visão do Leonardo).

### 3.10 Exploração SIGE a fundo
**52 endpoints GET.** `Genero` classifica melhor que Categoria (290 matéria-prima vs 60). `PrecosTabelas` = preços por canal dentro do produto. `LucroPercentual` = margem cadastrada (mas sobre custo). ⚠️ **`PrecoCusto` cadastrado é IRREGULAR/não-confiável** (0,01, 0,1 quebrados). `Lancamentos/GetAll` (financeiro — pode ter o custo de conversão que falta).

---

## 4. ESTADO ATUAL

| Frente | Estado |
|---|---|
| Integração SIGE read-only (cliente, deploy) | ✅ no ar (commit 44ce7f3) |
| Tela de Reconciliação SIGE | ✅ no ar, testada em produção |
| De-para 33 insumos + custos importados | ✅ feito (20 custos em prod) |
| Custo de produção · Margem · Vendas · Contribuição | ✅ motores prontos e testados (CLI) |
| Curva ABC por LUCRO completa | ⏳ 16,9% (falta custo dos formatos) |
| OPs / lotes / rendimento (leitura) | ✅ endpoint pronto, ❌ SIGE sem OPs ainda |
| Tela de Vendas no app | ❌ não começou |
| Custo de conversão (mão de obra/etc.) | ❌ gap — ninguém tem confiável |

---

## 5. ESCLARECIMENTOS E DISPARIDADES DA FÁBRICA (cravados)

- **"receita" = "tacho"** (mesma coisa). **Bandeja = 5,5 kg.**
- **DISPARIDADE ABERTA (Leonardo confirma):** quantas bandejas por tacho? Sistema assume **8 band × 5,5 kg = 44 kg**, mas a receita entra com **~33 kg** e o cozimento EVAPORA água → 44 de 33 é IMPOSSÍVEL; conta sugere **~4-5 band**. **Dobra o custo/unidade.**
- Matéria-prima vive sob **Soglia Indústria / depósito FABRICA**; é tudo UM local físico (CNPJs/depósitos são contábeis).

---

## 6. PERGUNTAS PENDENTES PRA FÁBRICA (priorizadas — destravam o resto)

1. **Rendimento do tacho:** quantas bandejas de 5,5 kg saem de 1 tacho de cocada? (8 vs ~4-5)
2. **Conversões dos formatos:** quantas unidades de Cubos 160g, Bala 400g, Pet, Pote, Mini saem de 1 tacho? (cobre os 83% das vendas sem custo)
3. **Custo de conversão:** a empresa tem o custo de mão de obra/embalagem/energia por produto? (pra margem virar lucro real; ver `Lancamentos` no SIGE)
4. **De-para §7:** caixa vs unidade (~7 insumos), manteiga vs margarina, qual leite condensado oficial. + cadastrar 5 insumos no SIGE.
5. **Requisitos gestora:** fichas técnicas viram fonte única no SIGE? quando começam a lançar OPs? (ver `docs/requisitos_gestora_sige.md`)
6. Docinhos: receita (pra completar as fichas).

---

## 7. PRÓXIMOS PASSOS (em ordem sugerida)

1. **Commit do que foi construído** (quando o Leonardo pedir): `custo_producao.py`, `margem_produto.py`, `vendas_sige.py`, `lucro_produto.py`, `sige_cloud_api.py` (leitura de pedidos), `entrevistas/precos_revenda_varejo_2026-06-15.md`. (push HF = autorização explícita).
2. **Tela de Vendas no app** (Curva ABC real por produto/canal/empresa — já temos os dados; não depende da fábrica). Validar no deploy (não rodar local).
3. **Expandir o de-para de venda** com as conversões dos formatos → Curva ABC por LUCRO completa.
4. **Custo de conversão** via `Lancamentos` do SIGE (explorar o financeiro) → margem real.
5. **OP de teste** quando a fábrica cadastrar → ler de volta, validar rendimento/lotes.
6. **Carga inicial de estoque** via contagem física → zera os negativos, dá vida à reconciliação.

---

## 8. ARQUIVOS-CHAVE (mapa)

| Arquivo | Função |
|---|---|
| `sige_cloud_api.py` | Cliente SIGE read-only (produtos, estoque, depósitos, empresas, OPs, **pedidos/vendas**, tabelas de preço). Escrita (produto/estoque/OP) isolada e bloqueada. |
| `importar_sige_api.py` | Ponte custo/identidade SIGE → insumos (read-only no SIGE). |
| `reconciliacao_sige.py` · `pages/13_Reconciliacao_SIGE.py` | Reconciliação estoque (consolidado) + tela. |
| `custo_producao.py` | Custo por produto (BOM × custo insumo). |
| `margem_produto.py` | Margem (preço − custo); DE_PARA_VENDA (produto→código SIGE→preço). |
| `vendas_sige.py` | Vendas do SIGE (Pedidos) → volume/receita/canal + Curva ABC. |
| `lucro_produto.py` | Vendas × custo = contribuição por produto. |
| `seed_bom_completa.py` | BOM (RECEITAS por tacho + INSUMOS). |
| `docs/ARQUITETURA_SIGE.md` · `requisitos_gestora_sige.md` · `fichas_tecnicas_para_sige.md` | Docs do SIGE. |
| `suprimentos_sigee/de_para_sige.md` | De-para dos 33 insumos (§7 = perguntas). |
| `entrevistas/precos_revenda_varejo_2026-06-15.md` | Preços atacado + varejo. |
| `audios/docs/transcricao_reuniao_gestora.md` | Transcrição das reuniões. |
| `_sige_*.py` / `_*.py` / `_*.json` | Scripts de exploração/análise (untracked, prefixo `_`). |
| `CLAUDE.md` · `CADERNO.md` | Regras · diário. |

---

## 9. FATOS TÉCNICOS / REGRAS CRÍTICAS

- **SIGE 100% read-only.** Escrita da OP existe (`/OrdensProducao/Cadastrar`) mas é o ramo PENDENTE (decisão da Gestão), isolado e bloqueado por `SIGE_PERMITIR_ESCRITA`.
- **Reconciliação usa o CONSOLIDADO** (GetAll EstoqueSaldo), não depósito isolado (fábrica = 1 local físico).
- **Vendas:** `Pedidos/Pesquisar` (pageSize MÁX 100; paginar). Itens em `Items`. Canal em `Tabela`. Filtrar StatusSistema "Faturado".
- **Genero** > Categoria pra achar insumo (290 matéria-prima).
- **Margem de MP engana** (alta sempre); custo total é o que importa, mas o PrecoCusto do SIGE é irregular → GAP do custo de conversão.
- **Rendimento do tacho** em disputa (8 vs ~4-5 band) → afeta custo unitário em 2×.
- **Cocada:** Zero NÃO tem 45g; Zero é alto volume mas A MAIS CARA de produzir. Bandeja 45g=100und. 1 tacho=8 band (Z=3) — A CONFIRMAR. Bandeja 5,5 kg.

---

## 10. MUDANÇAS NO TCC / ESTÁGIO (a desenvolver com o Leonardo)

O projeto **evoluiu muito** nesta fase: deixou de ser só "digitalizar a folha de produção" e virou um **PCP integrado ao ERP, com custo de produção, margem por canal, vendas reais e contribuição/lucro por produto**. Isso provavelmente **muda o escopo/título do TCC** (algo como "PCP integrado a ERP em confeitaria: do chão de fábrica ao custo e à margem") e pode ajustar o relatório de estágio. **Conversar com o Leonardo e o orientador sobre reposicionar.** Capítulos fortes novos: integração read-only (DDD/bounded contexts), reconciliação estoque (Forrester/3 camadas), custo de MP × custo total (a armadilha da margem), Curva ABC por lucro (não por volume). Lembrar: **docs acadêmicos = zero menção a IA.**

---

## 11. COMMITS DESTA SESSÃO

```
44ce7f3 feat(sige): integracao read-only — leitura OP/estoque, reconciliacao, fichas tecnicas
        (pushado origin + hf — tela de reconciliação no ar)
```
**NÃO commitado ainda** (aguarda pedido do Leonardo): `custo_producao.py`, `margem_produto.py`, `vendas_sige.py`, `lucro_produto.py`, `sige_cloud_api.py` (mudanças de pedidos/tabelas de preço), `entrevistas/precos_revenda_varejo_2026-06-15.md`, e os scripts `_*` de exploração (esses ficam de fora do commit).

---

## 12. MEMÓRIAS PERSISTENTES (índice em `~/.claude/.../memory/MEMORY.md`)
Chave desta fase: `project_sige_estado` (jornada SIGE → deploy → consolidado), `project_custo_margem` (custo/margem/rendimento), `project_sige_recursos` (52 endpoints, vendas, ciclo fechado), `feedback_persistir_dados` (cravar TUDO), `feedback_sem_nomes`, `feedback_comunicacao_simples`, `project_catalogo_produtos`, `project_receita_por_tacho`, `project_tcc_vs_relatorio_direcao`.

---

## 13. PRIMEIRA AÇÃO DA PRÓXIMA SESSÃO
1. Ler este handoff INTEIRO + as memórias + `CLAUDE.md`.
2. Confirmar que o SIGE conecta (`sige_cloud_api.testar_conexao()` — token no secrets local).
3. Situar-se: a cadeia SIGE→custo→margem→vendas→contribuição está montada (parcial); o que falta depende da fábrica (seção 6).
4. Perguntar ao Leonardo por onde seguir (não decidir sozinho) — provável: commit do que foi feito, ou tela de Vendas no app.

*Fim do handoff. A próxima sessão tem tudo aqui + memórias + CLAUDE.md.*
